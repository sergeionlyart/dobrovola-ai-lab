# Программа модуля

## Формат

Каждый этап состоит из одного часа совместной работы в Google Meet и двух часов самостоятельной лаборатории сразу после встречи.

| Этап | Онлайн | Самостоятельно | Контрольная точка |
|---|---:|---:|---|
| 1. Идея и контракт продукта | 60 мин | 120 мин | `SCOPE APPROVED` |
| 2. Агентная разработка | 60 мин | 120 мин | `VERTICAL WORKS` |
| 3. Аудит и релиз | 60 мин | 120 мин | `RELEASED` |

## Занятие 1 — от идеи к контракту

### На встрече

- показываем конечный продукт и всю цепочку артефактов;
- выбираем одного пользователя, одну проблему и одно целевое действие;
- проводим продуктовый диалог с ChatGPT;
- превращаем обсуждение в blueprint и критерии приёмки;
- разбираем назначение dev- и doc-репозиториев.

### Самостоятельная лаборатория

Студент создаёт и редактирует:

- `project-card.md`;
- `blueprint.md`;
- `copy.md`;
- `data-map.md`;
- `acceptance-criteria.md`.

Документы сохраняются в doc-репозитории. Результат должен быть достаточно узким, чтобы реализовать основной сценарий за оставшиеся четыре часа практики.

### Артефакт к занятию 2

Blueprint с одной аудиторией, одним CTA, одним backend-сценарием, явным `out of scope` и проверяемыми критериями.

## Занятие 2 — от blueprint к работающему сценарию

### На встрече

- создаём проект и milestones в Linear;
- учимся писать контракт задачи;
- разделяем роли Lead и Builder;
- показываем цикл issue → реализация → проверки → отчёт → приёмка;
- ограничиваем текущий milestone максимум четырьмя задачами.

### Самостоятельная лаборатория

Студент:

1. создаёт milestones `M0 Foundation`, `M1 Working Flow`, `M2 Release`;
2. просит Lead декомпозировать `M1`;
3. передаёт одну задачу Builder;
4. реализует страницу, тексты и форму;
5. проверяет полный локальный сценарий;
6. сохраняет commit или PR и отчёт исполнителя.

### Артефакт к занятию 3

Работающий локальный путь:

```text
страница → CTA → форма → валидация → подтверждение
```

В Linear есть задача с критериями и доказательствами, в GitHub — осмысленное изменение.

## Занятие 3 — аудит, релиз и самостоятельное изменение

### На встрече

- вводим роль Reviewer;
- сравниваем продукт с blueprint и критериями;
- разделяем критические и косметические дефекты;
- показываем исправление, повторную проверку и публикацию;
- объясняем итоговую сдачу.

### Самостоятельная лаборатория

Студент:

1. запускает независимый release review;
2. устраняет критические дефекты;
3. публикует продукт;
4. повторно проверяет production URL;
5. самостоятельно создаёт и проводит одно небольшое изменение через issue, commit, проверку и релиз;
6. заполняет `submission.md`.

## Что считается финалом

- доступен публичный URL;
- работает один полный пользовательский сценарий;
- документы проекта сохранены;
- видна цепочка blueprint → Linear issue → commit/PR → проверка;
- нет опубликованных секретов;
- выполнено самостоятельное изменение;
- студент может объяснить, что сделал агент и как результат был проверен.
